# Purpose: ms-a Deployment — may call ms-b only; receives calls from ms-c only.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ms-a
  namespace: osm-poc-demo
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-a
  namespace: osm-poc-demo
  labels:
    app: ms-a
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-a
  template:
    metadata:
      labels:
        app: ms-a
        version: v1
        topology.istio.io/network: main-network
      annotations:
        # Use Envoy sidecar (not ambient ztunnel) so the VM proxy (ms-c) can reach ms-a
        # via the EW gateway with regular mTLS instead of HBONE (HBONEPort=0 on EW GW).
        sidecar.istio.io/inject: "true"
        ambient.istio.io/redirection: disabled
    spec:
      serviceAccountName: ms-a
      containers:
        - name: ms-a
          image: quay.io/jwimsing/osm-poc-ms-a:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: PORT
              value: "8080"
            - name: MS_B_URL
              value: "http://ms-b.osm-poc-demo.svc.cluster.local:8080"
            - name: LOG_FORMAT
              value: "json"
            - name: JAVA_TOOL_OPTIONS
              value: "-Djava.util.logging.config.file=/dev/null"
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
